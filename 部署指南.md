# SimiGo LAN 节点部署指南（Mac #2 移植手册）

**适用：** Apple Silicon Mac（MLX 要求）；推荐 ≥32GB 统一内存运行 35B 级模型
**原则：** 逻辑隔离，执行共享，计算复用，预测治理，协议严格（白皮书 §65）

---

## 一、三步部署（MLX Native 后端，零外部依赖）

### Step 1 · 构建

```bash
git clone https://github.com/slamfinger/SimiGo.git && cd SimiGo
xcodebuild build -project SimiGo.xcodeproj -scheme SimiGo \
  -configuration Release -destination 'platform=macOS'
# 产物：
# ~/Library/Developer/Xcode/DerivedData/SimiGo-*/Build/Products/Release/SimiGo.app
```

### Step 2 · 获取模型（二选一）

```bash
# 路线 A：新机直接下载（推荐，约 20GB）
huggingface-cli download peculiar-ragdoll/Nail-Qwen3.6-35B-A3B-MLX

# 路线 B：从旧机整目录拷贝（含 blobs 硬链接结构，用 rsync 保留）
rsync -av ~/.cache/huggingface/hub/models--peculiar-ragdoll--Nail-Qwen3.6-35B-A3B-MLX \
  新机:~/.cache/huggingface/hub/
```

### Step 3 · 启动与配置

1. 启动 SimiGo.app（菜单栏图标）
2. 选择模型 → 指向 snapshot 目录（`~/.cache/huggingface/hub/models--*/snapshots/<hash>/`）
3. 网络模式切到 **LAN**（绑定 0.0.0.0；日志确认 `LAN=true`）
4. 验证：`curl http://<本机LAN-IP>:8000/v1/models`

节点参数（端口/绑定/Bonjour）见应用内设置；模型级配置（temp/topP/maxTokens）按模型路径分别持久化。

---

## 二、llama.cpp 后端（可选，仅 GGUF 模型需要）

MLX Native 模式**不需要** llama.cpp。若需运行 GGUF 模型：

- **应用内引导**：设置页触发环境构建（自动 git clone + cmake Metal 构建，需 Xcode CLT + cmake）；
- **手动**：`brew install llama-server`（EnvManager 会依次探测 Homebrew/系统/`~/.simigo/bin`）。

## 三、移植性说明（代码审计结论）

| 项 | 状态 | 说明 |
|---|---|---|
| 权重感知 KV 预算 | ✅ 自适应 | 自动按新机 RAM 与模型权重体积计算（NativeMLX init 量取） |
| 路径假设 | ✅ 全部 `$HOME` 相对 | `~/.simigo/{bin,models,logs}` 自动创建 |
| llama-server 探测 | ✅ 优雅降级 | Homebrew → 系统 → binDir，找不到给出明确指引 |
| 双后端分发 | ✅ | `.mlx` → NativeMLX（进程内）；`.gguf` → llama-server（外部进程） |
| 模型路径失效 | ⚠️ 已守卫 | start 前置存在性检查，返回"❌ 不存在"；**手动选择无效目录会崩溃（Detector fatalError，边界情况待软化）** |
| LAN 鉴权 | ⚠️ 无 | 节点为无鉴权 OpenAI 兼容 API，仅家庭/可信网络使用 |

## 四、部署后验证清单

```bash
curl http://<LAN-IP>:8000/v1/models                      # 模型列表
curl http://<LAN-IP>:8000/health 2>/dev/null || true     # 存活
# 并发冒烟：两台设备同时各发一个请求，观察日志
tail -f ~/.simigo/logs/native_mlx_trace.log
#   [GENERATION START] ... decodeStreams=N   ← 并发流数
#   [KVTRIM] est_freed=...                   ← 会话常驻自动修剪
#   [ADMISSION OBS] weights= budget=         ← 权重感知预算自适应
```

## 五、多节点拓扑建议

- **按模型分工**：35B 节点跑重 Agent，7–14B 节点承接轻量高并发；
- **客户端侧路由**：OpenAI 兼容客户端配置 `base_url` 指向不同节点即可，节点间无耦合；
- **一致性**：各节点独立 Global KV 池（节点内复用），跨节点不复用——按会话粘性路由可最大化命中率。
